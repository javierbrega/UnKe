// NOTE: This is a placeholder for the Supabase client wrapper
export const createClient = () => {
    return {
        // Mocked supabase instance
        auth: {},
        from: (table: string) => ({})
    }
}
